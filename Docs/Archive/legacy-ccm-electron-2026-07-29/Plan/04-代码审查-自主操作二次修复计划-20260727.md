# 04 · 代码审查：自主操作二次修复计划

> **日期**：2026-07-27  
> **对照**：Plan/02（执行 SSOT）+ Plan/03（P0 首轮）+ 二次审查  
> **结论（已验证）**：首轮 P0 护栏方向正确但不完整；本计划封住崩溃空账、路径越界、saveAs basename 回归、刷新前静默写删。

---

## 1. 二次审查增补（按严重度）

| ID | 严重度 | 一句话 |
|---|---|---|
| R1 | 严重 | catalog 写盘中途崩溃 → 主文件缺失 → 下次启动写空账，忽略 `.bak`/`.tmp` |
| R2 | 严重 | `libraryPath` 无库根 containment → 可读写删库外 |
| R3 | 严重 | `copyMissingIntoLibraryFromScan` 改回 basename → saveAs 覆盖回归 |
| R4 | 高 | 刷新 fullSync 在决议前对账/软登记/可能删库 |
| R5 | 高 | `catalogHealthy` 设在 infer 之后 → 同进程修复后无法恢复 |
| R6 | 高 | `deployedPath` 无受管容器校验 → 可删库外普通文件 |
| R7 | 高 | saveAs/overwrite/retire 删账与删盘顺序有不一致窗口 |
| R8 | 门禁 | typecheck 失败；restore 脚本过期；U4 verify 假阳性 |

---

## 2. 执行清单

### P0

- [x] R1/R5 catalog 崩溃恢复 + healthy 顺序 + 迁移前护栏
- [x] R2/R6 `resolveLibrarySafePath` + `assertManagedContainerPath`
- [x] R3/R7 saveAs 事务 + 禁止 basename 覆盖
- [x] R4 快照/刷新预览零写删

### P1

- [x] 批量结果 / typecheck / verify 门禁
- [x] 哈希 memo + 单次对账

### P2

- [x] openPath allowlist / Docs 收口

---

## 3. 验收

见工作区 Cursor Plan「自主操作二次修复」验收节。

**验证（已跑）**：`npm run typecheck`；`node scripts/verify-autonomous-ops-guards.mjs`；`node scripts/verify-move-restore-conflicts.mjs`；`npm run verify:all`。

**版本**：v1.1 | **状态**：已落地
