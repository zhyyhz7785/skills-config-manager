---
name: L1G90-software-product-discovery
description: >-
  在动手写代码前，为「要做一款什么软件、怎么做」做系统的产品立项调研与规划，分五步：
  (0) 锁定目标功能 / 目标用户 / 平台 / 约束；
  (1) 强制联网搜索同类与竞品软件，覆盖商业、开源、SaaS、移动 App、浏览器插件、CLI/库等各形态，列成清单；
  (2) 从「功能、用户体验与易用性、可靠与安全、稳定性、差异化」五个维度打分对比（默认短表，勿堆长文）；
  (3) 据此推导应当做一款什么样的软件（市场空白、定位、目标用户、MVP 功能集 MoSCoW、非功能目标）；
  (4) 给出最优实现路径（自研 / Fork 开源 / 套用库或 API 的取舍、技术选型、架构概要、分阶段路线图、风险与对策）。
  产出写入 {DOCS}/Research/（命名 编号-标题-YYYY-MM-DD.md）。
  触发场景：用户说「我想做一个 XX 软件 / 做个 XX 工具 / 这类软件有哪些 / 竞品调研 / 市场对标 /
  该做成什么样 / 怎么实现最优 / 产品立项 / 需求调研」，或在写第一行实现代码前需要先想清楚做什么、怎么做时。
  **模式 B 触发**：已有产品/扩展内「某个 UI 控件放哪、怎么切换、显示效果对比」——如编辑器标题栏 vs 状态栏 vs 块内工具栏；
  用户说「显示效果不太好 / 对比选最优 / UI 专项 / 交互方案对比」且上下文是**现有代码**而非从零立项时。
  模式 A 只产出调研与规划，不直接写实现代码；**模式 B 在对比表落盘后可直接改代码**（见 §模式 B）。
  产出须遵守本文写作纪律；模板完整度不得压过写作纪律。
---

# 软件产品调研 → 实现路径规划

在为某个功能「从零做一款软件」之前，先把**做什么**和**怎么做**想清楚。本 skill 把流程固化为 5 个阶段，最终落盘一份调研报告。

## 引用门禁

**结论（建议）**：单独拷走也能跑完。可必读：本目录内文件。禁必读：其他 skill 目录与 `.cursor/rules/`。分流只写职责，不写「先读某 SKILL.md」。落盘写入 `{DOCS}/Research/`。

* **范围**：模式 A 到「最优实现路径 / 技术方案规划」为止，**不写实现代码**；模式 B 到「方案对比 + 选定 + 最小实现」为止，**可改代码**。

* **诚信**：竞品、价格、版本、活跃度等事实**必须来自联网搜索或只读核对**；查不到标 **待验证**，**禁止编造**。

***

## 写作纪律（自包含）

**结论**：阶段清单与报告模板从属于本文写作纪律；模板填不满可以，**写作/证据自检不过禁止落盘**。

优先级：本文写作纪律 > 本 skill 流程 / 模板完整度。

按「查实 → 说话 → 收尾」顺序执行。各条辖区不重叠，例外已写进条目内部；不存在需要跨条仲裁的情况。

### 查实（写之前）

1. 禁止虚构文件、代码行为、测试结果、规范、数值、来源、工具输出。不确定先读代码/文档；工具失败如实报告，不得改称成功。
2. 绝对路径、版本、单位、坐标系、外部规范：先核对再写出。
3. 我的前提有误或方案高风险时，直接指出技术原因，不迎合。
4. 动笔前按目的定分析取向：为了**懂** → 第一性原理（自下而上），从不可再拆的起点往上重建，拆开黑箱、学原理、建知识架构；为了**选** → 用户逻辑（自上而下，别名顶层逻辑），从用户/客户场景倒推最佳实现，技术当黑箱，对比方案与竞品，不进实现细节。本 skill 做**选**，产出写入 `{DOCS}/Research/`。

### 说话（写的时候）

5. 自然语言一律简体中文；代码、路径、命令、专有名称保持原文。英文名称或英文术语在每份文档首次出现时，后面加（专业中文译名）；再次出现不加。
6. 第一句 = 可画的结论（可画 = 纸上画得出来的话；本条定义，全文复用）；精确展开跟在后面。复杂问题按「事实→抽象→模型/规则→预测/应用→新的事实」展开；事实含前提与边界——前提是已知的门槛，边界是未知的、要靠机制揭示出来的门槛。文首「总论」与每个 `##` 节先给结论，再写支撑事实；不为凑齐模板章节而注水。
7. 术语准入三分：可画且精确 → 只用可画语言，不上术语；可画但不够精确 → 先可画描述，术语跟在后面；不可画 → 直接用术语。
8. 已出现的术语或缩写：首次出现紧跟「是什么＋起什么作用」，缩写给全称；表格格内首现同样；再次出现不重复。这段解释不算冗长，任何简短要求不得删它。
9. 除术语解释外尽量简短：避免格式化、形式化、重复表达；不为形式完整下钻，我点名「详细」才展开。表格只在对比 ≥ 3 项或批量解释术语时用。默认短报告、以能判断取舍为上限。
10. 近义词不并排堆；不得不同时出现时，逐词一行「词 = 本质 ＋ 与相邻词的差别」，本质区别第一眼可见。
11. 类比先讲原理并标明边界；结构工程行话只在本轮谈结构时用。
12. 关键结论标一档：已验证 / 推断 / 建议 / 待验证。未实测不得称「通过」。
13. 本轮我未点名写、改、删某份面向用户的文档或交付物（非源码、非标准配置）时，不创建、不改写、不删除它，收尾不顺手补。
14. 新建面向用户的文档或交付物（非源码、非标准配置），且我未指定名、项目无命名惯例、工具无固定名时，命名为 `{序号}-{文件名}-{yyyyMMdd}.{扩展名}`，序号 = 目标目录内同类文件最大序号 + 1；固定名（README.md、LICENSE、SKILL.md，以及项目约定的 README、01～05 等）不加序号日期。本 skill 调研报告命名见阶段 5。

### 收尾（交付前）

15. 能从代码、文档、项目约定查明的先自查；仅当选择实质影响结果且自查无法判定才提问，一次至多 3 个。不复述规则、不解释常规流程。
16. 内部自检、不输出过程：第一句是可画结论吗？英文首现有译名、术语都解释了吗？关键结论都标级别了吗？有改文档则本轮点名了吗？任一不过，先改再交。

**禁止**用「阶段清单已打勾 / 模板章节齐全 / 命令能跑」代替上述验收。报告与聊天**不粘贴本文写作纪律全文**（用户质疑遵守或要求审改时除外）。

### 落盘

竞品/立项调研写入 `{DOCS}/Research/`；命名见阶段 5；**不**以 `product-discovery/` 为默认（避免第 6 子目录）。阶段 2 以表+一行总评为主。阶段 0 能自查则标「假设」；实质分歧才问且一次≤3。查不到用 **待验证**，禁止「待核实」作为唯一标签。

***

## 工作流总览

复制此清单并逐项推进（一个阶段做完再进下一个）：

```
- [ ] 阶段 0：锁定目标功能、目标用户、平台形态、约束、五维权重（缺省标假设；提问≤3）
- [ ] 阶段 1：联网搜索竞品（各形态全覆盖），列成竞品清单
- [ ] 阶段 2：五维打分 + 汇总对比表（默认短评；勿每竞品长文）
- [ ] 阶段 3：从分析推导产品定义（空白、定位、MVP、非功能目标、差异化锚点）
- [ ] 阶段 4：最优实现路径（构建方式取舍、技术选型、架构、路线图、风险）
- [ ] 阶段 5：按 Research 命名落盘到 {DOCS}/Research/（先过写作纪律）
```

***

## 模式 B：现有产品 UI / 交互专项（轻量）

**结论**：模式 B 流程更短，**不豁免**本文写作纪律。

**何时用**：产品/扩展已存在；用户反馈「显示效果差」「切换入口不明显」；需在 **2–5 个 UI 方案** 间选型并落地。不重走完整竞品立项，但**仍须联网查 3+ 参考**（同类 IDE/编辑器怎么放按钮）。

**与模式 A 的区别**：

| | 模式 A（立项） | 模式 B（UI 专项） |
|---|---|---|
| 范围 | 整款产品 | 单个控件/布局/切换流 |
| 竞品数量 | 5–12（清单可多，正文深写少） | 3–6 个参考实现 |
| 产出 | `{DOCS}/Research/` 调研报告 | 对比表 + 可选短 memo；**直接改代码** |
| 权重 | 五维按阶段 0 | **易用 40% + 与宿主一致 30% + 功能 20% + 实现成本 10%** |

**工作流（复制推进）**：

```
- [ ] B0：锁定「谁在哪一步要做什么」+ 宿主约束（VS Code / WPF / Web…）
- [ ] B1：联网查 3+ 参考（官方文档优先：如 VS Code editor/title、Markdown 预览）
- [ ] B2：列 2–5 个候选方案，按 B 权重打分
- [ ] B3：选最优 + 写清「不选其余方案的原因」一句（节首先给推荐结论）
- [ ] B4：最小实现 + 目视验收标准（1–3 条）；未实测不称「已通过」
```

**方案对比表模板**（UI 专项必用；表内术语首现须解释）：

| 方案 | 做法 | 易用 | 宿主一致 | 功能 | 成本 | 加权 | 结论 |
|---|---|---|---|---|---|---|---|
| A | … | /5 | /5 | /5 | /5 | | |
| B | … | | | | | | **推荐** |

**HyMD 已验证样例（2026-07）**：

| 需求 | 候选 | 结论 |
|---|---|---|
| 整篇 WYSIWYG ↔ 源码 | 状态栏 / 命令面板 / **editor/title 图标** / webview 内嵌 | **editor/title**（与 Markdown 预览同位，可发现性最高） |
| sheet 块 预览 ↔ 块体 YAML | Crepe 原生 Copy 栏 / 块工具栏「源码」 / 双击块 | **块工具栏「源码↔预览」** + 修复 Crepe DOM 装饰（`.milkdown-code-block`） |
| 块卡片未渲染根因 | 只查 `pre code` | Crepe 用 `div.milkdown-code-block`，须双路径采集 |

**模式 B 落盘（可选）**：短 memo 写入业务仓既有核心文档一节，或 `{DOCS}/Research/` 短文；**禁止**新建 `{DOCS}` 下第 6 个子目录（如 `product-discovery/`）。用户只要改 UI 时，以代码 + 简短结论回复即可（结论先行、术语首现）。

***

## 阶段 0：锁定目标与约束（搜索前必做）

**结论**：不先锁定目标就开搜 = 方向跑偏；缺项先自查再给默认并标「假设」，勿连环追问。

先对齐下列项；用户没说的，**给出合理默认并显式标注「假设」**。仅当选择实质影响结果且自查无法判定时提问，**一次至多 3 个**。

| 项 | 要确认什么 |
|---|---|
| 核心功能 | 一句话说清「这款软件帮谁，解决什么问题」 |
| 目标用户 | 谁用、专业度、规模、付费意愿 |
| 平台形态 | 桌面(Win/Mac/Linux) / Web / 移动(iOS/Android) / 浏览器插件 / CLI / 库·SDK（Software Development Kit，软件开发工具包）/ CAD·IDE 插件 |
| 关键约束 | 开源还是商业、预算与人力、时间窗口、技术栈偏好、离线/在线、合规要求 |
| 成功标准 | 怎样算「做成了」（可给默认假设） |
| 五维权重 | 按目标定权重（工具类→易用性高；金融/医疗→安全高；高频生产→稳定高），合计 100% |

**输出**：一段「目标陈述」+ 一张五维权重表。

***

## 阶段 1：竞品搜索（强制联网）

**结论**：形态要搜全，正文不必写全；清单可宽，深写留给阶段 2 的头部竞品。

用 `WebSearch` 系统性搜索，**中英文都搜**，年份用当前年。目标是穷尽「同类 + 可替代 + 邻域参考」，避免只盯一两个知名产品。

**查询模板**（按主题替换关键词）：

* `best {功能/品类} software` / `{品类} 软件 推荐 对比`
* `{品类} open source github` / `{品类} 开源 替代`
* `{已知竞品} alternatives` / `{已知竞品} 替代品`
* `{品类} reddit` / `{品类} 知乎 / V2EX 对比评测`
* `{品类} review G2 / Capterra / Product Hunt / AlternativeTo`

**形态全覆盖清单**（每类至少**搜**一次；无命中可空，勿编造）：

```
- [ ] 商业桌面/客户端软件
- [ ] 开源项目（GitHub / GitLab）
- [ ] SaaS / Web 应用
- [ ] 移动 App（iOS / Android）
- [ ] 浏览器插件 / IDE·CAD 插件
- [ ] CLI 工具 / 库 / SDK
- [ ] 间接替代（用别的方式解决同一痛点，如 Excel、手工流程）
```

**每个竞品记录字段**：

| 名称 | 形态 | 平台 | 商业模式·价格 | 开源? | 活跃度(最近更新) | 链接 | 一句话定位 |
|---|---|---|---|---|---|---|---|

价格、版本、星数等须核对来源；不确定标 **待验证**。

**数量**：主流 5–12 个列入清单；额外可标 1–2 个「小众强者」。**归类**：直接竞品 / 间接替代 / 邻域参考。

***

## 阶段 2：五维度分析（默认短写）

**结论**：默认用汇总表决策；禁止为形式完整给每个竞品写长优缺点。

对清单中的直接竞品（及必要时关键间接替代）按五维各打 **1–5 分**（1=差，5=优）。证据尽量来自官网/文档/真实用户评价；主观处标 **推断**。

**默认正文形态**：

1. 竞品清单表（阶段 1）
2. 汇总对比表（含加权总分 + 一行总评，总评含证据级别）
3. 行业现状小结（普遍好 / 普遍短板）各数句
4. **仅对加权总分前 3～5 个直接竞品**可各附极短优缺点（**各 ≤3 条**）
5. 其余竞品：清单一行即可，不堆同质段落

用户明确要求「详细 / 细致」时，再扩大优缺点篇幅。

**五维评估子项（rubric）**：

1. **功能（Functionality）**：核心覆盖、专业深度、边界能力、集成与扩展、平台广度。
2. **用户体验与易用性（UX & Usability）**：上手成本、界面与信息密度、高频效率、文档、本地化、无障碍。
3. **可靠与安全（Reliability & Security）**：数据安全/合规、认证权限、备份、可审计、漏洞响应。
4. **稳定性（Stability）**：崩溃与 Bug、大数据表现、资源占用、维护节奏、兼容性。
5. **差异化（Differentiation）**：独特卖点、护城河、价格模式、生态、数据可迁移、人群细分。

> 「差异化」评竞品时 = 相对其他产品的独特护城河；喂给阶段 3 找空白。

**汇总对比表**（加权总分 = Σ 各维得分 × 阶段 0 权重）：

| 竞品 | 功能 | 易用 | 可靠安全 | 稳定 | 差异化 | 加权总分 | 一句话总评（含证据级别） |
|---|---|---|---|---|---|---|---|

***

## 阶段 3：产品定义——该做什么样的软件

**结论**：从普遍短板与空白格反推定位；必须有差异化锚点与不做清单，避免更差的复制品。

1. **机会矩阵**：用户高频痛点 × 竞品满足度（高/中/低/空白）→ 圈空白/低满足 = 切入点。
2. **一句话定位**：为【目标用户】提供【核心价值】，区别于【主要竞品】在于【差异化锚点】。
3. **目标用户画像**：主要 + 次要（场景、痛点、当前替代）。
4. **MVP 功能集（MoSCoW）**——MoSCoW：Must / Should / Could / Won't，是按优先级切割范围的方法，作用是锁定最小可行产品（MVP，Minimum Viable Product，最小可行产品，作用是用最少功能验证价值）。

| 优先级 | 含义 | 功能 |
|---|---|---|
| Must | 没有它产品不成立 | … |
| Should | 重要但 MVP 后可补 | … |
| Could | 锦上添花 | … |
| Won't(now) | 本期明确不做 | … |

5. **非功能目标**：可量化（性能/可靠/安全/易用），对齐想超越的维度。
6. **差异化锚点**（≥1）：成本 / 体验 / 垂直 / 开放生态 / 可迁移 / AI / 工具链集成等。
7. **反向清单**：明确不做什么。

***

## 阶段 4：最优实现路径

**结论**：「最优」= 最小风险与成本、最快交付差异化；先构建方式，再技术细节；高风险须直接指出。

**① 构建方式决策**（优先复用，避免无脑自研）：

| 方式 | 适用 | 代价 |
|---|---|---|
| 从零自研 | 差异化在核心引擎、现有方案均不可复用 | 最高成本、最高可控 |
| Fork 成熟开源二次开发 | 已有接近底座，差异化在上层 | 省底座，受架构与许可约束 |
| 套用库/引擎/API 做壳 | 核心能力可被现成库/云 API 覆盖 | 最快，供应商/依赖锁定 |
| 现成 SaaS/低代码拼装 | 验证期、内部工具、非核心 | 最省力，定制与迁移受限 |

给出**推荐 + 理由**，并列 1 个备选。

**② 技术选型**：默认 + 备选；勿堆长串；核对**许可证**与商业用途。版本号须核对来源。

**③ 架构概要**：模块 / 数据流 / 风险点（一段话 + 必要时短列表，**不写实现代码**）。

**④ 路线图**：MVP → V1 → V2；每阶段交付物 + 验收点；最痛功能先做。验收点未实测标 **待验证**。

**⑤ 风险与对策**：

| 风险 | 类型(技术/许可/依赖/市场) | 影响 | 对策 |
|---|---|---|---|

**⑥ 假设验证**：最危险假设优先（原型、访谈、技术 PoC——Proof of Concept，概念验证，作用是低成本试通假设）。

***

## 阶段 5：落盘报告

**结论**：调研类产出进 Research，遵守项目文档结构；**不要**默认创建 `product-discovery/`。

1. **确定 `{DOCS}`**：仓库既有 `Docs/` 或 `docs/`；用户 `@` 了主题目录或已有多主题 → 用该主题下的 `{DOCS}`（如 `docs/FiniteElement/`）。
2. **目录**：`{DOCS}/Research/`（不存在则创建；**禁止**因此新增 `{DOCS}` 下第 6 个子目录）。
3. **序号**：扫描该 `Research/` 下现有 `编号-*.md`，取最大编号 +1；无文件时从 `01` 或与目录既有习惯一致的起始编号。
4. **文件名**：`{编号}-{主题}-YYYY-MM-DD.md`（日期用当天；写入 `{DOCS}/Research/`）。
5. **历史说明**：旧路径 `docs/product-discovery/` 或 `Docs/product-discovery/` **不再作为默认**；已有文件不强制本任务迁移。
6. 按下方模板写入；事实附来源链接；不确定标 **待验证**。
7. **先过本文写作纪律与文末自检**，通过后再落盘。

***

## 报告模板

```markdown
# {软件主题} 产品调研与实现路径

> 调研日期：{YYYY-MM-DD}
> 目标形态：{桌面/Web/移动/插件/CLI/库}
> 一句话目标：{帮谁，解决什么问题}

## 总论

**结论（建议|推断|已验证|待验证）**：{做成什么样、靠什么差异化、第一步先做什么——先写结论}

## 0. 目标与约束

**结论（…）**：{目标与权重一句话}

- 核心功能：…
- 目标用户：…（缺省已标「假设」）
- 平台形态：…
- 关键约束：…
- 成功标准：…
- 五维权重：功能 _% / 易用 _% / 可靠安全 _% / 稳定 _% / 差异化 _%（合计 100%）

## 1. 竞品清单

**结论（…）**：{直接/间接/邻域各有哪些、为何够用}

| 名称 | 形态 | 平台 | 商业模式·价格 | 开源? | 活跃度 | 链接 | 一句话定位 |
|---|---|---|---|---|---|---|---|
（分组：直接竞品 / 间接替代 / 邻域参考；表内术语首现须解释，如 SaaS（Software as a Service，通过网络按订阅使用软件））

## 2. 五维度分析

**结论（…）**：{谁总分领先、普遍短板是什么}

### 汇总对比

| 竞品 | 功能 | 易用 | 可靠安全 | 稳定 | 差异化 | 加权总分 | 总评（含证据级别） |
|---|---|---|---|---|---|---|---|

### 头部竞品要点（仅前 3～5 名；各≤3 条优/缺点，非详细要求时勿加长）

### {竞品 A}
- 优点：…
- 缺点：…
- 评分：功能 _/5 · 易用 _/5 · 可靠安全 _/5 · 稳定 _/5 · 差异化 _/5

### 行业现状小结
- 普遍做得好：…
- 普遍短板/痛点：…

## 3. 我应当做的产品

**结论（建议）**：{一句话定位}

- 机会矩阵 / 切入空白：…
- 一句话定位：为【】提供【】，区别于【】在于【】
- 目标用户画像：主 / 次
- MVP 功能集（MoSCoW：Must/Should/Could/Won't，按优先级切割范围）：…
- 非功能目标（可量化）：…
- 差异化锚点：…
- 不做清单：…

## 4. 最优实现路径

**结论（建议）**：{推荐构建方式一句话；高风险直接点明}

- 构建方式：推荐【】，理由【】；备选【】
- 技术选型：语言/UI/存储/关键依赖（默认 + 备选）+ 许可证核对（版本已核对）
- 架构概要：模块 / 数据流 / 风险点
- 路线图：MVP → V1 → V2（交付物 + 验收点；未实测标待验证）
- 风险与对策：表
- 假设验证：最危险假设 + 验证手段

## 5. 结论

**结论（建议）**：{做成什么样、靠什么差异化、第一步先做什么}

---

**版本**：v1.0 | **更新**：{YYYY-MM-DD}
```

***

## 自检清单（落盘阻断）

下列任一项失败 → **禁止落盘**，先改正。自检内部完成，不向用户输出过程。

**写作纪律**

```
- [ ] 文首总论与各 ## 节有节首结论
- [ ] 术语/缩写首现已「是什么＋起什么作用」（含表格格）
- [ ] 关键结论已标 已验证/推断/建议/待验证；无未实测称「通过」
- [ ] 无「为填满模板」的空洞段落；未要求详细则篇幅以可判断取舍为限
```

**调研质量**

```
- [ ] 阶段 0 目标与五维权重已确认（缺省已标「假设」）；本轮提问≤3
- [ ] 竞品清单 ≥ 3 且搜索覆盖多种形态（不是清一色只搜一类）
- [ ] 有汇总对比表 + 加权总分 + 行业现状小结
- [ ] 头部 3～5 竞品有短评或分数依据；未对清单内每一项强制长优缺点
- [ ] 产品定义含「差异化锚点」与「不做清单」
- [ ] 实现路径先做了构建方式取舍（评估过 Fork/复用）
- [ ] 技术选型有默认+备选并核对许可证；版本/价格/路径已核对或标待验证
- [ ] 路线图最痛功能先做 + 最危险假设的验证手段；高风险已直指
- [ ] 竞品事实有来源链接，无编造
```

**落盘**

```
- [ ] 写入 {DOCS}/Research/，文件名 编号-标题-YYYY-MM-DD.md
- [ ] 未新建 {DOCS} 下第 6 个子目录（含勿默认创建 product-discovery/）
```

***

## 反模式（不要这样做）

* **用本 skill 模板完整度压过写作纪律** → 禁止落盘。
* **先落盘再补术语 / 节首结论** → 禁止。
* **每个竞品长文优缺点**（用户未要求详细）→ 禁止。
* **默认写入 `product-discovery/`** 或新增 `{DOCS}` 第 6 子目录 → 禁止。
* **报告或回复复述本文写作纪律全文** → 禁止（审改时除外）。
* **跳过阶段 0** 直接搜竞品 → 方向跑偏。
* **只列功能、不打分、不对比** → 看不出差距与机会。
* **竞品只搜一种形态** → 漏掉开源/SaaS/插件强者。
* **产品定义没有差异化锚点** → 更差的复制品。
* **实现路径默认从零自研** → 未评估 Fork/复用。
* **模式 A 内直接写实现代码** → 越界；模式 B 允许改代码。
* **编造竞品、价格、用户数、测试通过** → 禁止；查不到标 **待验证**。

***

**写作与落盘以本文写作纪律为准｜2026-08-24**
